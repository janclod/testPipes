---
title: "testPipes"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{testPipes}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(testPipes)
```

## DESCRIPTION

This package simply aims to reproduce an error encountered while working
on a bigger [project](https://github.com/informalr/informalr/)
when using pipes (%>%).

In short,
GHA [fails](https://github.com/informalr/informalr/actions/runs/442815899)
to build on macOS and Ubuntu when pipes are used in more than one single file.

## TWO SEPARATE R FILES

This package contains two R scripts (function1.R and function2.R).

function1 returns the output of x

```{r}
function1(201989)
```

function2 also returns the output of y

```{r}
function2("example")
```
